<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout - RedSeam Clothing</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'primary': '#FF4000',
                        'secondary': '#10151F',
                        'accent': '#3E424A',
                        'light-gray': '#E1DFE1',
                        'dark-gray': '#6B7280'
                    }
                }
            }
        }
    </script>
    
    <!-- Remix Icons -->
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <link href="/styles/checkout.css" rel="stylesheet">
    <link href="/styles/cart.css" rel="stylesheet">
</head>
<body class="bg-white">
    <!-- Header -->
    <% if (typeof user !== 'undefined' && user) { %>
        <%- include('partials/headerLogin') %>
    <% } else { %>
        <%- include('partials/headerNotLogin') %>
    <% } %>

    <!-- Main Content -->
    <main class="min-h-screen" style="padding-left: 100px; padding-right: 100px;">
        <!-- Checkout Title Section -->
        <div style="padding-top: 72px;">
            <h1 style="font-size: 42px; color: #10151F; font-weight: 600; margin: 0;">Checkout</h1>
        </div>

        <!-- Order Details Section -->
        <div style="padding-top: 42px;">
            <div style="display: flex; gap: 24px;">
                <!-- First Subsection - Order Details Card -->
                <div class="order-details-card" style="width: 1129px; height: 635px; background-color: #F8F6F7; border-radius: 16px; padding-left: 47px; padding-top: 72px;">
                    <!-- Order Details Title -->
                    <h2 style="font-size: 22px; color: #10151F; font-weight: 500; margin: 0 0 46px 0;">Order details</h2>
                    
                    <!-- Form Fields -->
                    <div style="display: flex; flex-direction: column; gap: 20px;">
                        <!-- First Row - Name and Surname -->
                        <div style="display: flex; gap: 16px;">
                            <div>
                                <input type="text" placeholder="Name" style="width: 277px; height: 42px; padding: 12px 16px; border: 1px solid #E1DFE1; border-radius: 8px; font-size: 16px; outline: none; background-color: white;">
                            </div>
                            <div>
                                <input type="text" placeholder="Surname" style="width: 277px; height: 42px; padding: 12px 16px; border: 1px solid #E1DFE1; border-radius: 8px; font-size: 16px; outline: none; background-color: white;">
                            </div>
                        </div>
                        
                        <!-- Second Row - Email -->
                        <div>
                            <input type="email" placeholder="Email" style="width: 570px; height: 42px; padding: 12px 16px; border: 1px solid #E1DFE1; border-radius: 8px; font-size: 16px; outline: none; background-color: white;">
                        </div>
                        
                        <!-- Third Row - Address and Zip Code -->
                        <div style="display: flex; gap: 16px;">
                            <div>
                                <input type="text" placeholder="Address" style="width: 277px; height: 42px; padding: 12px 16px; border: 1px solid #E1DFE1; border-radius: 8px; font-size: 16px; outline: none; background-color: white;">
                            </div>
                            <div>
                                <input type="text" placeholder="Zip code" style="width: 277px; height: 42px; padding: 12px 16px; border: 1px solid #E1DFE1; border-radius: 8px; font-size: 16px; outline: none; background-color: white;">
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Second Subsection - Cart Summary (without header) -->
                <div style="flex: 1; position: relative;">
                    <!-- Cart Items Section (same as cart sidebar but without header) -->
                    <div id="checkout-cart-items" style="margin-top: 0; display: flex; flex-direction: column; height: 635px; overflow-y: auto; padding-right: 10px;">
                        <!-- Cart items will be loaded here by JavaScript -->
                    </div>
                    
                    <!-- Cart Summary (same as cart sidebar) -->
                    <div class="cart-summary" style="position: absolute; bottom: 150px; left: 0; right: 0; background: white; z-index: 10;">
                        <div class="cart-summary-item">
                            <span class="cart-summary-label">Items subtotal</span>
                            <span class="cart-summary-value" id="checkout-items-subtotal">$ 0</span>
                        </div>
                        <div class="cart-summary-item">
                            <span class="cart-summary-label">Delivery</span>
                            <span class="cart-summary-value">$ 5</span>
                        </div>
                        <div class="cart-summary-total">
                            <span class="cart-summary-label">Total</span>
                            <span class="cart-summary-value" id="checkout-total">$ 5</span>
                        </div>
                    </div>
                    
                    <!-- Checkout Button (same as cart sidebar) -->
                    <div class="cart-checkout" style="position: absolute; bottom: 10px; left: 0; right: 0; display: flex; justify-content: center; background: white; z-index: 10;">
                        <button class="cart-checkout-btn" onclick="processCheckout()" style="width: 460px; height: 59px; background-color: #FF4000; color: white; border: none; border-radius: 10px; font-size: 18px; font-weight: 500; cursor: pointer; transition: background-color 0.2s ease;">Pay</button>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Scripts -->
    <script>
        // Pass token to client-side JavaScript
        const serverToken = '<%= typeof token !== "undefined" ? token : "" %>';
        
        if (serverToken && serverToken !== '') {
            sessionStorage.setItem('userToken', serverToken);
        }
    </script>
    <script src="/scripts/checkout.js"></script>
    <script src="/scripts/cart.js"></script>
</body>
</html>
